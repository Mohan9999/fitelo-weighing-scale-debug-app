plugins {
    // AGP 9.x has built-in Kotlin support — the org.jetbrains.kotlin.android
    // plugin is no longer applied (removed per the AGP 9 migration guide).
    id("com.android.application") version "9.2.0" apply false
}
